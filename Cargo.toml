[workspace]
resolver = "2"
members = ["src-tauri"]

[workspace.package]
version = "0.1.0"
edition = "2021"

# El crate `core` (dominio headless: reloj, linea temporal, feedforward) se anade
# como miembro durante la implementacion del harness, siguiendo TDD estricto
# (Constitucion, Principio II).
